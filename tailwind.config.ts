import type { Config } from "tailwindcss";

// Tailwind v4 primarily uses CSS-based theming via @theme.
// This config remains minimal and reserved for future extensions.
const config: Config = {
	// Intentionally empty; tokens are defined in src/styles/tokens.css
};

export default config;
